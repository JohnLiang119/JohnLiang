# JohnLiang 工作區開發與流程規範 (AI Guidelines)

## 🌐 語言與溝通規範
1. 自然語言溝通、註解、文件（如 `README.md`, `CLAUDE.md`）與 commit message 一律使用**繁體中文**。
2. 思考過程亦保持繁體中文。

## 💻 程式碼與檔案編碼規範
1. **檔案編碼（重要）**：所有檔案（特別是 C# `*.cs`、PowerShell `*.ps1`、批次檔 `*.bat` 與 Markdown `*.md`）存檔時必須一律使用 **UTF-8 with BOM**。
2. **C# 版本相容性**：舊版 .NET 4.0 編譯環境不支援 C# 6.0 的字串插值（`$` 語法），一律使用 `string.Format()`。
3. **程式碼邏輯**：程式碼命名與變數保留原始英文，註解說明一律使用繁體中文。

## ⚙️ OpenSpec 開發流程規範
1. **禁止重複產生計畫檔案**：所有實作細節與計畫一律撰寫於 OpenSpec 的 `proposal.md` 中，嚴禁在 `.gemini` 目錄下產生重複的 `implementation_plan.md`。
2. **任務進度同步**：任務執行完畢時，務必更新對應的 `tasks.md`，將狀態標記為已完成（`[x]`）。
3. **執行權限**：Agent 執行時，需要運行的程序與指令可直接執行，無需再次詢問。
